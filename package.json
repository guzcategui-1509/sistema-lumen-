{
  "name": "lumen-workspace",
  "version": "0.1.0",
  "private": true,
  "description": "Lumen Workspace MVP interno para ordenes de trabajo, equipo y notificaciones.",
  "scripts": {
    "dev": "python3 -m http.server 4174 --bind 127.0.0.1",
    "check": "node --check app.js && node --check phase-reorder.js && node --test tests/phase-reorder.test.js && node --test tests/work-order-conversation-loading.test.js && node --test tests/work-order-phase-assignment-preservation.test.js && node --test tests/brand-creation.test.js"
  },
  "keywords": [
    "lumen",
    "workspace",
    "work-orders"
  ],
  "license": "UNLICENSED"
}
